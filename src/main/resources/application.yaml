spring:
  application:
    name: BuyOrNot
  datasource:
    url: jdbc:mysql://localhost:3306/buy_or_not?useSSL=false&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: spring       # TODO: DB 사용자명 수정
    password: 1234      # TODO: DB 비밀번호 수정
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  thymeleaf:
    cache: false
